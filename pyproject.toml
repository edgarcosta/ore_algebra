[build-system]
requires = [
  'setuptools',
  'Cython', 
  'pkgconfig',
  # Note: passagemath dependencies are conditionally added in setup.py
  # to avoid binary incompatibility with existing Sage installations
]
build-backend = "setuptools.build_meta"
